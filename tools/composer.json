{
    "name": "root/tools",
    "license": "MIT",
    "type": "project",
    "require": {
        "php": ">=8.1 <=8.5",
        "nette/tester": "^2.5",
        "tracy/tracy": "^2"
    },
    "config": {
        "platform": {
            "php": "8.1.0"
        }
    },
    "scripts": {
        "test": "vendor/bin/tester -C -p php -s tests"
    }
}
